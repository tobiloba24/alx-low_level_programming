#include <stdio.h>

/**
 * main- entry point
 *
 * Return: should be 0 when succesful
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}

